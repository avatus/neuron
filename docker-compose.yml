version: '3.4'
services:
    postgres:
        image: postgres:10.4
        restart: always
        ports:
            - '55432:5432'
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: neuron_local
